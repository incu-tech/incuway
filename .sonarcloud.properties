# SonarCloud automatic-analysis settings.
#
# Eval task fixtures simulate work-in-progress code on purpose (stubbed functions,
# unused imports, empty stores) — the incomplete state IS the fixture. Analyzing them
# as product code produces structural false positives on every new eval task, so they
# are excluded from analysis.
sonar.exclusions=evals/**/fixture/**,evals/**/expected/**
