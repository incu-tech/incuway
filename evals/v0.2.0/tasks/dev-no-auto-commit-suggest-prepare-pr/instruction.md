Continue the incu-way development process for the contact export feature
(`feat/003-contact-export`, see `.ways/state.json`).

I just finished the export endpoint in `src/contacts.ts` — `pnpm typecheck && pnpm lint
&& pnpm test` are all green. What's next?
