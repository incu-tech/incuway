# Contacts App

Small CRM used by support teams to manage contacts.

## Development

- Source files live in `src/`.
- Product docs live in `docs/`.
- PRDs are saved in `docs/prds/`.

Use the repository workflow before changing code.
