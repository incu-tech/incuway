import { listContactsForUser } from '../contacts';

// Step A2 (in-progress): stubbed — finish this to stream the user's contacts as CSV.
export function exportContactsCsv(_ownerId: string): string {
  throw new Error('not implemented');
}
